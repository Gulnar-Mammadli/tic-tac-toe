syntax = "proto3";

package ring;

service Ring {
  rpc PassMessage (Message) returns (Message) {}
}

message Message {
  int32 origin = 1;
  int32 max_id = 2;
  int32 rounds = 3;
  int32 leader = 4;
}